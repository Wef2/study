# Mysqli
```php
<?php

$server = "server";
$user = "user";
$password = "password";
$db = "mydb";

// Create connection
$conn = new mysqli($server, $user, $password, $db);

// Close connection
$conn->close();

//

?>
```
